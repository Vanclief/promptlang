--- a/codex-rs/tui/src/bottom_pane/mod.rs
+++ b/codex-rs/tui/src/bottom_pane/mod.rs
@@ -159,6 +159,7 @@
 mod pending_input_preview;
 mod pending_thread_approvals;
 pub(crate) mod popup_consts;
+mod promptlang;
 mod scroll_state;
 mod selection_popup_common;
 mod selection_row_layout;
--- a/codex-rs/tui/src/bottom_pane/chat_composer.rs
+++ b/codex-rs/tui/src/bottom_pane/chat_composer.rs
@@ -4975,7 +4975,13 @@
                     .textarea
                     .render_ref_masked(textarea_rect, buf, &mut state, mask_char);
             } else {
-                let mut highlights = self.plugin_at_mention_highlights();
+                let mut highlights = if self.draft.is_bash_mode {
+                    Vec::new()
+                } else {
+                    super::promptlang::condition_highlights(self.draft.textarea.text())
+                };
+                // Keep native mention and search styles above PromptLang's conditions.
+                highlights.extend(self.plugin_at_mention_highlights());
                 let search_highlight_style =
                     Style::default().add_modifier(Modifier::REVERSED | Modifier::BOLD);
                 highlights.extend(
@@ -5044,6 +5050,10 @@
 #[cfg(test)]
 #[path = "chat_composer/agents_navigation_tests.rs"]
 mod agents_navigation_tests;
+
+#[cfg(test)]
+#[path = "chat_composer_promptlang_tests.rs"]
+mod promptlang_tests;

 #[cfg(test)]
 #[path = "chat_composer_effort_tests.rs"]
