#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
binary="$repo_root/.build/target/dev-small/codex"
if [[ ! -x "$binary" ]]; then
    printf 'Build PromptLang first: %s/scripts/build.sh\n' "$repo_root" >&2
    exit 1
fi
exec "$binary" "$@"
